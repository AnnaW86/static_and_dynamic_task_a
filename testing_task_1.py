class CardGame:

  def check_for_ace(self, card):
    if card.value = 1: # should be '=='
      return True
    else  # should be 'else:'
      return False
   

  dif highest_card(self, card1 card2): #dif should be def; should be comma between card1 and card2
  if card1.value > card2.value: # should be indented, with lines 13-15 also indented once more
    return card #should be card1
  else: # should be an additional if statement, eg card1.value < card2.value so as not to include the scenario of the cards' values being the same
    return card2
        # should now have the else, or an == condition for when the values are the same
  


def cards_total(self, cards): # whole method should be indented
  total # this needs an initial value. based on the context, should be 'total = 0'
  for card in cards:
    total += card.value
    return "You have a total of" + total # should not be indented inside for loop, and should be a formatted string
  